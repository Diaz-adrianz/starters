# server 
MODE=development
PORT=5001

SERVER_URL=http://localhost:5001

# tokens 
TOKEN_VERIFICATION_EXPIRE=86400 # 1d in s
TOKEN_RESETPASSWORD_EXPIRE=900 # 15m in s

# device id
DEVICE_ID_SECRET=
DEVICE_ID_EXPIRE=31536000 # 1y in s

# databases
DATABASE_DEFAULT_HOST=localhost
DATABASE_DEFAULT_PORT=5432
DATABASE_DEFAULT_USERNAME=
DATABASE_DEFAULT_PASSWORD=
DATABASE_DEFAULT_NAME=

# jwt auth
JWT_ACCESS_SECRET=
JWT_ACCESS_EXPIRE=900 # 15m in s
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRE=604800 # 7d in s
JWT_ISSUER=adrianz

# caches
CACHE_DEFAULT_HOST=localhost
CACHE_DEFAULT_PORT=6379

# loggers
LOGGER_DEFAULT_PATH=logs/

# mailers 
MAIL_TEMPLATES_PATH=src/common/mail/templates/
MAIL_HOST=
MAIL_PORT=
MAIL_SENDER=
MAIL_USER=
MAIL_PASS=
MAIL_SECURE=

# misc 
SEED_USERS_PASSWORD=
