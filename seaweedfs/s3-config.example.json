{
	"identities": [
		{
			"name": "cli",
			"credentials": [
				{
					"accessKey": "${SEAWEEDFS_CLI_ACCESS_KEY_ID}",
					"secretKey": "${SEAWEEDFS_CLI_SECRET_ACCESS_KEY}"
				}
			],
			"actions": ["Admin", "Read", "Write", "List", "Tagging"]
		},
		{
			"name": "service-name",
			"credentials": [
				{
					"accessKey": "",
					"secretKey": ""
				}
			],
			"actions": ["Read", "Write", "List", "Tagging"],
			"resources": ["arn:aws:s3:::bucket-name", "arn:aws:s3:::bucket-name/*"]
		}
	]
}

